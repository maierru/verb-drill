<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="<%= meta_description %>">
    <link rel="canonical" href="<%= canonical_url %>">
    <meta name="robots" content="index, follow">
    <meta property="og:title" content="<%= title %>">
    <meta property="og:description" content="<%= meta_description %>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= canonical_url %>">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="<%= title %>">
    <meta name="twitter:description" content="<%= meta_description %>">
    <link rel="stylesheet" href="../assets/css/index.css">
</head>
<body>
    <div class="container">
        <header>
            <a href="../index.html" class="back-link"><%= back_link %></a>
            <h1><%= h1 %></h1>
            <p class="subtitle"><%= subtitle %></p>
        </header>

        <div class="verb-list">
<% verbs.each do |verb| -%>
            <div class="verb-card">
                <div class="verb-info">
                    <div class="verb-name"><%= verb[:name] %></div>
                    <div class="verb-meaning"><%= verb[:meaning] %></div>
                </div>
                <div class="verb-langs">
<% verb[:langs].each do |lang| -%>
                    <a href="<%= verb[:slug] %>/<%= lang[:code] %>.html" class="lang-badge"><%= lang[:label] %></a>
<% end -%>
                </div>
            </div>

<% end -%>
        </div>

        <footer>
            <p><%= footer %> · <a href="https://github.com/maierru/verb-drill">GitHub</a> · <a href="https://apps.apple.com/pt/app/verb-drill/id6756883141?l=en-GB">iOS App</a></p>
        </footer>
    </div>
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "ItemList",
        "name": "<%= title %>",
        "description": "<%= meta_description %>",
        "numberOfItems": <%= verbs.length %>,
        "itemListElement": [
<% verbs.each_with_index do |verb, i| -%>
            {"@type": "ListItem", "position": <%= i + 1 %>, "name": "<%= verb[:name] %>", "url": "<%= site_url %>/<%= lang_folder %>/<%= verb[:slug] %>/en.html"}<%= i < verbs.length - 1 ? ',' : '' %>
<% end -%>
        ]
    }
    </script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name %></title>
    <link rel="stylesheet" href="assets/css/index.css">
</head>
<body>
    <div class="container">
        <header>
            <h1><%= name %></h1>
            <p class="subtitle"><%= tagline %></p>
        </header>

        <div class="verb-list">
<% languages.each do |lang| -%>
            <div class="verb-card">
                <div class="verb-info">
                    <div class="verb-name"><%= lang['flag'] %> <%= lang['name'] %></div>
                    <div class="verb-meaning">Learn <%= lang['name'] %> verbs</div>
                </div>
                <div class="verb-langs">
                    <a href="<%= lang['path'] %>" class="lang-badge">Start</a>
                </div>
            </div>

<% end -%>
        </div>

        <footer>
            <p><%= name %> — <a href="<%= github_url %>">Open source</a> language practice</p>
        </footer>
    </div>
</body>
</html>

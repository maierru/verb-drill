# <%= name %>

<%= description %>.

## What is this?

Flashcards for mastering verb conjugations. Pick a language you want to learn, then your mother tongue — phrases appear in your language, you translate mentally, click to reveal the answer.

## Languages to learn

<% languages.each do |lang| -%>
- <%= lang['flag'] %> [<%= lang['name'] %>](<%= site_url %>/<%= lang['code'] %>/)
<% end -%>

More coming.

## Mother tongues supported

<% origins.each do |o| -%>
- <%= o['flag'] %> <%= o['name'] %>
<% end -%>

## How many phrases per verb?

<%= phrases_per_verb %>, sorted by frequency. Covers all tenses from basic present to subjunctive.

## Offline?

Yes. Static HTML, no backend. Save the page or clone the repo.

## Contributing

PRs welcome. See `.claude/CLAUDE.md` for structure and how to add verbs.
